#include <iostream>
using namespace std;

int main()
{
    double a, b, c, d;
    do
    {
        cout << "Nhap a:";
        cin >> a;
    } while (a <= 0 || a >= 200);
    do
    {
        cout << "Nhap a:";
        cin >> a;
    } while (a <= 0 || a >= 200);
    do
    {
        cout << "Nhap a:";
        cin >> a;
    } while (a <= 0 || a >= 200);
    do
    {
        cout << "Nhap a:";
        cin >> a;
    } while (a <= 0 || a >= 200);
}